<UserControl x:Class="PilotObjectInfo.Views.OrgUnitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PilotObjectInfo.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding OrganisationUnits}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" 
                                    Binding="{Binding Id, Mode=OneWay}"/>
                <DataGridTextColumn Header="Title"
                                    Binding="{Binding Title, Mode=OneWay}"/>
                <DataGridTextColumn Header="IsDeleted"
                                    Binding="{Binding IsDeleted, Mode=OneWay}"/>
                <DataGridTextColumn Header="IsPosition"
                                    Binding="{Binding IsPosition, Mode=OneWay}"/>
                <DataGridTextColumn Header="IsChief"
                                    Binding="{Binding IsChief, Mode=OneWay}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
